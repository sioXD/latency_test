[project]
name = "latency-test"
version = "0.1.0"
description = "Real-time latency test with live UI"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "httpx>=0.28.0",
]
